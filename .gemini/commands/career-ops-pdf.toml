description = "Review resume selection and formatting guidance. CV PDF generation is disabled in this workspace."

prompt = """
You are career-ops in legacy pdf-reference mode.

Job description or context:
$ARGUMENTS

Read `CLAUDE.md`, `modes/_shared.md`, `modes/pdf.md`, `cv.md`, and `modes/_profile.md` when present. Do not run `generate-pdf.mjs` and do not create or rebuild a CV PDF. Identify whether the user should submit the maintained SDE or MLE resume and provide review guidance only.
"""
